import React, { useState, useEffect } from 'react';
import { 
  Camera, 
  Calendar, 
  TrendingUp, 
  Users, 
  CheckCircle, 
  Play, 
  Instagram, 
  Linkedin, 
  Mail, 
  Menu, 
  X,
  ArrowRight,
  MapPin,
  Video,
  Clapperboard,
  Clock,
  DollarSign,
  Star,
  ChevronRight,
  FileText,
  Smartphone,
  BarChart3,
  Quote,
  Lightbulb,
  Target,
  Zap,
  GraduationCap,
  Building2,
  Film,
  Award,
  BookOpen,
  PieChart,
  Layers,
  ArrowLeft,
  Youtube,
  ExternalLink,
  ShieldCheck,
  Rocket
} from 'lucide-react';

// --- Custom Professional Logo Component (SVG based to prevent flicker) ---
const ICANLogo = ({ className = "h-12" }) => (
  <div className={`flex items-center gap-2 ${className}`}>
    <div className="relative h-10 w-10 bg-red-600 rounded-xl flex items-center justify-center shadow-lg shadow-red-600/30 overflow-hidden group">
      <Clapperboard className="text-white w-6 h-6 z-10 transition-transform group-hover:rotate-12" />
      <div className="absolute inset-0 bg-gradient-to-tr from-black/20 to-transparent"></div>
    </div>
    <div className="flex flex-col leading-none">
      <span className="text-xl font-black tracking-tighter text-inherit">
        ICAN<span className="text-red-600">FILM</span>
      </span>
      <span className="text-[8px] font-bold uppercase tracking-[0.3em] opacity-60">Agency & Academy</span>
    </div>
  </div>
);

// --- Robust Video Player Component ---
const VideoLoop = ({ type, id, title }) => {
  if (type === 'youtube') {
    return (
      <div className="relative w-full h-full overflow-hidden rounded-[2rem] shadow-2xl bg-black group">
        <iframe
          className="absolute top-1/2 left-1/2 w-[150%] h-[150%] -translate-x-1/2 -translate-y-1/2 pointer-events-none object-cover"
          src={`https://www.youtube.com/embed/${id}?autoplay=1&mute=1&loop=1&playlist=${id}&controls=0&modestbranding=1&rel=0&iv_load_policy=3&enablejsapi=1`}
          title={title}
          allow="autoplay; encrypted-media"
        ></iframe>
        <div className="absolute inset-0 bg-black/10 group-hover:bg-transparent transition-colors"></div>
      </div>
    );
  }

  // Mux Video Implementation using standardized mp4 stream for preview
  return (
    <div className="relative w-full h-full overflow-hidden rounded-[2rem] shadow-2xl bg-slate-900 group">
      <video
        autoPlay
        muted
        loop
        playsInline
        className="absolute top-0 left-0 w-full h-full object-cover"
        poster={`https://image.mux.com/${id}/thumbnail.jpg?width=1200&time=2`}
      >
        <source src={`https://stream.mux.com/${id}/medium.mp4`} type="video/mp4" />
        Your browser does not support the video tag.
      </video>
      <div className="absolute inset-0 bg-gradient-to-t from-slate-900/40 to-transparent group-hover:opacity-0 transition-opacity"></div>
    </div>
  );
};

// --- Page Content Components ---

const HomePage = ({ navigateTo }) => (
  <div className="animate-fade-in">
    {/* Hero Section */}
    <section className="relative h-screen min-h-[800px] flex items-center bg-slate-900 overflow-hidden">
      <div className="absolute inset-0 bg-gradient-to-b from-slate-900/40 via-slate-900/70 to-slate-900 z-10"></div>
      <div className="absolute inset-0 z-0 overflow-hidden">
         <img 
          src="https://images.unsplash.com/photo-1492691527719-9d1e07e534b4?ixlib=rb-1.2.1&auto=format&fit=crop&w=1950&q=80" 
          alt="Production" 
          className="w-full h-full object-cover scale-105 animate-slow-zoom"
        />
      </div>

      <div className="container mx-auto px-6 relative z-20 pt-20">
        <div className="max-w-4xl">
          <div className="inline-flex items-center gap-2 bg-red-600/20 text-red-400 px-4 py-2 rounded-full text-sm font-black mb-8 border border-red-600/30 backdrop-blur-md">
            <span className="w-2 h-2 bg-red-500 rounded-full animate-pulse"></span>
            CALGARY'S EVENT-TO-CONTENT SPECIALISTS
          </div>
          <h1 className="text-6xl md:text-8xl lg:text-9xl font-black text-white leading-[0.9] mb-8 tracking-tighter">
            PRODUCE <br /><span className="text-red-600 italic">RESULTS.</span><br />
            NOT JUST <span className="text-transparent bg-clip-text bg-gradient-to-r from-red-500 to-orange-400">FILM.</span>
          </h1>
          <p className="text-xl md:text-2xl text-gray-300 mb-10 max-w-2xl leading-relaxed font-medium">
            Turn your one-day events into 30 days of high-conversion content. We build the engine that keeps your brand visible while you run your business.
          </p>
          <div className="flex flex-col sm:flex-row gap-6">
            <button onClick={() => navigateTo('booking')} className="bg-red-600 hover:bg-red-700 text-white px-10 py-6 rounded-2xl font-black text-xl transition-all transform hover:scale-105 flex items-center justify-center gap-3 shadow-2xl shadow-red-600/40 uppercase tracking-widest">
              Claim Free Audit <ArrowRight size={24} />
            </button>
            <button onClick={() => navigateTo('portfolio')} className="bg-white/10 hover:bg-white/20 backdrop-blur-xl text-white border border-white/20 px-10 py-6 rounded-2xl font-black text-xl transition-all flex items-center justify-center gap-3 uppercase tracking-widest">
              <Play size={24} className="fill-current" /> Our Work
            </button>
          </div>
          
          <div className="mt-20 flex flex-wrap gap-12 items-center opacity-50 grayscale hover:grayscale-0 transition-all duration-700">
            <div className="text-white font-black text-2xl italic tracking-tighter">RE/MAX</div>
            <div className="text-white font-black text-2xl tracking-[0.2em]">FIT45</div>
            <div className="text-white font-serif text-2xl italic">The Bistro</div>
            <div className="text-white font-mono text-2xl">CHAMBER</div>
          </div>
        </div>
      </div>
    </section>

    {/* Strategy Section */}
    <section className="py-32 bg-white">
      <div className="container mx-auto px-6">
        <div className="grid lg:grid-cols-2 gap-20 items-center">
          <div>
            <h2 className="text-5xl md:text-7xl font-black text-slate-900 mb-8 leading-none tracking-tighter">
              Stop Posting <br /><span className="text-red-600 italic">Sporadically.</span>
            </h2>
            <p className="text-xl text-gray-600 mb-12 leading-relaxed">
              Calgary business owners often procrastinate on social media because it feels like a chore. We turn that chore into a system that yields massive ROI.
            </p>
            <div className="grid grid-cols-2 gap-8">
              <div className="space-y-2">
                 <p className="text-5xl font-black text-red-600 tracking-tighter">89%</p>
                 <p className="text-sm font-bold text-slate-500 uppercase tracking-widest">Video ROI Rating</p>
              </div>
              <div className="space-y-2">
                 <p className="text-5xl font-black text-slate-900 tracking-tighter">300%</p>
                 <p className="text-sm font-bold text-slate-500 uppercase tracking-widest">Reach via Repurposing</p>
              </div>
            </div>
          </div>
          <div className="bg-slate-900 rounded-[3rem] p-12 text-white shadow-3xl relative overflow-hidden">
             <div className="absolute top-0 right-0 w-64 h-64 bg-red-600/10 rounded-full blur-3xl -translate-y-20 translate-x-20"></div>
             <h3 className="text-2xl font-bold mb-8 italic">The ICAN Content Loop</h3>
             <div className="space-y-10 relative z-10">
                {[
                  { step: "01", title: "Capture Once", desc: "A monthly deep-dive session or event coverage.", icon: <Camera className="text-red-500" /> },
                  { step: "02", title: "Multiply Often", desc: "Raw footage becomes 15+ high-impact assets.", icon: <Layers className="text-red-500" /> },
                  { step: "03", title: "Dominate Daily", desc: "Automated schedule keeps you top-of-mind.", icon: <Rocket className="text-red-500" /> }
                ].map((item, i) => (
                  <div key={i} className="flex gap-6">
                    <div className="text-red-600 font-black text-xl opacity-30">{item.step}</div>
                    <div>
                      <div className="flex items-center gap-2 mb-1">
                        {item.icon}
                        <h4 className="font-bold text-xl">{item.title}</h4>
                      </div>
                      <p className="text-gray-400 text-sm">{item.desc}</p>
                    </div>
                  </div>
                ))}
             </div>
          </div>
        </div>
      </div>
    </section>
  </div>
);

const ServicesPage = ({ navigateTo }) => (
  <div className="animate-fade-in pt-32 pb-20">
    <div className="container mx-auto px-6">
      <div className="text-center max-w-4xl mx-auto mb-24">
        <h1 className="text-6xl md:text-8xl font-black text-slate-900 mb-8 tracking-tighter">Scale Your <br /><span className="text-red-600 italic underline decoration-red-100 underline-offset-8">Visibility.</span></h1>
        <p className="text-xl text-gray-500 leading-relaxed font-medium">From low-risk entry points to full-scale content engines, we have a system for every growth stage.</p>
      </div>

      <div className="grid lg:grid-cols-3 gap-10 max-w-7xl mx-auto">
        {/* Tier 1 */}
        <div className="bg-white p-12 rounded-[2.5rem] shadow-xl border border-gray-100 flex flex-col hover:border-red-500 transition-all group">
          <div className="bg-red-50 text-red-600 w-16 h-16 rounded-2xl flex items-center justify-center mb-10 group-hover:bg-red-600 group-hover:text-white transition-all shadow-lg shadow-red-100"><Lightbulb size={32} /></div>
          <h3 className="text-3xl font-black mb-4 tracking-tight">The Audit</h3>
          <p className="text-red-600 font-black mb-6 uppercase tracking-widest text-xs">Foot-in-the-door Offer</p>
          <p className="text-gray-500 mb-10 flex-grow leading-relaxed">A high-value strategy session to review your current presence and give you 3-5 immediate content ideas. No pressure, just proof.</p>
          <button onClick={() => navigateTo('booking')} className="w-full py-5 border-2 border-slate-900 text-slate-900 font-black rounded-2xl hover:bg-slate-900 hover:text-white transition-all uppercase tracking-widest text-xs">Start for Free</button>
        </div>

        {/* Tier 2 - Flagship */}
        <div className="bg-slate-900 p-12 rounded-[2.5rem] shadow-3xl text-white flex flex-col relative transform scale-105 border-4 border-red-600">
          <div className="absolute top-0 right-0 -mt-5 mr-10 bg-red-600 text-[10px] font-black tracking-[0.2em] px-5 py-2 rounded-full uppercase shadow-xl">Flagship System</div>
          <div className="bg-red-600 text-white w-16 h-16 rounded-2xl flex items-center justify-center mb-10 shadow-xl shadow-red-600/30"><Zap size={32} /></div>
          <h3 className="text-3xl font-black mb-4 tracking-tight">Monthly Retainer</h3>
          <p className="text-red-400 font-black mb-6 uppercase tracking-widest text-xs italic">From $800 / Month</p>
          <p className="text-gray-400 mb-10 flex-grow leading-relaxed italic">Our 70% energy focus. We become your content department. Capture, Edit, Deliver, Repeat.</p>
          <ul className="space-y-5 mb-12">
            <li className="flex items-center gap-3 text-sm font-bold"><CheckCircle className="text-red-500" size={20} /> On-Site Content Day</li>
            <li className="flex items-center gap-3 text-sm font-bold"><CheckCircle className="text-red-500" size={20} /> 4-8 Polished Video Reels</li>
            <li className="flex items-center gap-3 text-sm font-bold"><CheckCircle className="text-red-500" size={20} /> 20+ Professional Stills</li>
            <li className="flex items-center gap-3 text-sm font-bold"><CheckCircle className="text-red-500" size={20} /> Full Content Calendar</li>
          </ul>
          <button onClick={() => navigateTo('booking')} className="w-full py-5 bg-red-600 text-white font-black rounded-2xl hover:bg-red-700 transition-all shadow-2xl shadow-red-600/30 uppercase tracking-widest text-xs">Claim Your Spot</button>
        </div>

        {/* Tier 3 */}
        <div className="bg-white p-12 rounded-[2.5rem] shadow-xl border border-gray-100 flex flex-col hover:border-slate-900 transition-all group">
          <div className="bg-slate-100 text-slate-900 w-16 h-16 rounded-2xl flex items-center justify-center mb-10 group-hover:bg-slate-900 group-hover:text-white transition-all shadow-lg shadow-slate-100"><Camera size={32} /></div>
          <h3 className="text-3xl font-black mb-4 tracking-tight">Event Coverage</h3>
          <p className="text-red-600 font-black mb-6 uppercase tracking-widest text-xs">Selective Projects</p>
          <p className="text-gray-500 mb-10 flex-grow leading-relaxed">High-profile gala or conference coverage. We treat every one-off job as a Trojan horse to prove our wider content system.</p>
          <button onClick={() => navigateTo('booking')} className="w-full py-5 border-2 border-slate-900 text-slate-900 font-black rounded-2xl hover:bg-slate-900 hover:text-white transition-all uppercase tracking-widest text-xs">Inquire for Event</button>
        </div>
      </div>
    </div>
  </div>
);

const PortfolioPage = ({ navigateTo }) => {
  const portfolioItems = [
    {
      client: "Gems Cup Calgary",
      type: "Cinematic Event Coverage",
      title: "High-Stakes National Horse Competition",
      problem: "Premier horse competition with business leaders from across Canada. Needed coverage that matched the prestige of the event.",
      solution: "Full cinematic deployment with drone operations and high-end 4K ground cameras. Captured keynote speeches and competition intensity.",
      result: "Produced a high-prestige highlight series used for sponsor recaps and 2026 promotion. Loop showcases our aerial and ground scale.",
      videoType: 'youtube',
      videoId: 'Z-4poApHgkE',
      side: "left"
    },
    {
      client: "Estavio Real Estate Group",
      type: "Weekly Education Retainer",
      title: "Authority Building for Realtors",
      problem: "Weekly training events with industry experts and law firms needed to be repurposed to build brand authority online.",
      solution: "Consistent capture of speaker sessions and networking. Transformed technical knowledge into digestible, viral social assets.",
      result: "Built Estavio into a top-of-mind educational brand in the Calgary real estate market. Constant flow of recruitment-ready content.",
      videoType: 'mux',
      videoId: 'gJzbHrC01LBX00QmdojNxoA00L2ncGo02auPd46DKqqEmKM',
      side: "right"
    },
    {
      client: "MOA Training Program",
      type: "Education Marketing",
      title: "Medical Office Assistant Recruitment",
      problem: "A vocational program needing to clearly communicate student life and career outcomes through high-energy storytelling.",
      solution: "Focus on the 'Day in the Life' of a student. Created a high-pacing marketing piece that highlights professionalism and hands-on tools.",
      result: "Significant boost in digital inquiries. The cornerstone marketing asset for their quarterly enrollment campaigns.",
      videoType: 'mux',
      videoId: 'tHQPOaEigvny7OAkC5yAfxGd7cykNJEdMF2vUXLolXc',
      side: "left"
    }
  ];

  return (
    <div className="animate-fade-in pt-32 pb-20">
      <div className="container mx-auto px-6">
        <div className="text-center max-w-3xl mx-auto mb-24">
          <h1 className="text-6xl font-black text-slate-900 mb-6 tracking-tighter">Our Proof.</h1>
          <p className="text-xl text-gray-600">Real Calgary brands. Real production quality. Real results.</p>
        </div>
        <div className="space-y-48 mb-24">
          {portfolioItems.map((item, i) => (
            <div key={i} className={`flex flex-col ${item.side === 'right' ? 'lg:flex-row-reverse' : 'lg:flex-row'} items-center gap-20`}>
              <div className="lg:w-[55%] w-full h-[600px]">
                 <VideoLoop type={item.videoType} id={item.videoId} title={item.client} />
              </div>
              <div className="lg:w-[45%]">
                <div className="inline-block bg-red-600 text-white font-black text-[10px] uppercase tracking-[0.3em] px-4 py-1.5 rounded-full mb-6 italic">{item.type}</div>
                <h3 className="text-4xl md:text-5xl font-black mb-8 italic tracking-tighter leading-none">{item.client}</h3>
                <div className="space-y-8 mb-10">
                  <div>
                    <h4 className="font-black text-gray-900 text-xs uppercase tracking-widest mb-2 opacity-40">The Challenge</h4>
                    <p className="text-gray-600 leading-relaxed font-medium">{item.problem}</p>
                  </div>
                  <div>
                    <h4 className="font-black text-gray-900 text-xs uppercase tracking-widest mb-2 opacity-40">The Solution</h4>
                    <p className="text-gray-600 leading-relaxed font-medium">{item.solution}</p>
                  </div>
                </div>
                <div className="bg-slate-900 p-8 rounded-[2rem] border border-slate-800 flex items-start gap-4 shadow-2xl">
                  <div className="bg-red-600 p-2 rounded-lg text-white shadow-lg shadow-red-600/30"><Rocket size={24} /></div>
                  <p className="text-white font-bold leading-tight">{item.result}</p>
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  );
};

const WorkshopsPage = ({ navigateTo }) => (
  <div className="animate-fade-in pt-32 pb-20">
    <div className="container mx-auto px-6">
      <div className="text-center max-w-4xl mx-auto mb-24">
        <div className="inline-block bg-red-100 text-red-600 font-black px-4 py-1 rounded-full text-[10px] uppercase tracking-[0.3em] mb-4 italic">Lead Generation Funnel</div>
        <h1 className="text-6xl md:text-8xl font-black text-slate-900 mb-8 tracking-tighter leading-none">Cinepreneur <br /><span className="text-red-600 italic underline decoration-red-100 underline-offset-8">Academy.</span></h1>
        <p className="text-xl text-gray-500 leading-relaxed font-medium">Empowering Calgary businesses to build internal content muscle through modular expertise.</p>
      </div>

      <div className="grid lg:grid-cols-3 gap-10 mb-32">
        <div className="bg-slate-900 text-white p-12 rounded-[2.5rem] shadow-2xl flex flex-col hover:-translate-y-2 transition-all relative overflow-hidden group">
          <div className="absolute top-0 right-0 w-32 h-32 bg-red-600/10 rounded-full blur-3xl -translate-y-10 translate-x-10"></div>
          <Building2 className="text-red-500 mb-8 relative z-10" size={56} />
          <h3 className="text-3xl font-black mb-4 italic tracking-tight relative z-10">Corporate & Staff</h3>
          <p className="text-gray-400 mb-10 leading-relaxed relative z-10">Train your in-house marketing department to capture professional daily wins without outsourcing every shoot.</p>
          <button onClick={() => navigateTo('corporate-training')} className="mt-auto py-5 bg-white text-slate-900 font-black rounded-2xl uppercase tracking-widest text-[10px] relative z-10 hover:bg-red-600 hover:text-white transition-colors">Detailed Training Track</button>
        </div>

        <div className="bg-white p-12 rounded-[2.5rem] shadow-xl border border-gray-100 flex flex-col hover:border-red-600 transition-all relative overflow-hidden group">
          <Target className="text-red-600 mb-8" size={56} />
          <h3 className="text-3xl font-black mb-4 italic tracking-tight">Business Strategy</h3>
          <p className="text-gray-500 mb-10 leading-relaxed">Master the 'Content Buckets' method and the 1-hour content planning framework used by industry leaders.</p>
          <button onClick={() => navigateTo('business-brands')} className="mt-auto py-5 border-2 border-slate-900 text-slate-900 font-black rounded-2xl uppercase tracking-widest text-[10px] hover:bg-slate-900 hover:text-white transition-colors">Detailed Strategy Track</button>
        </div>

        <div className="bg-white p-12 rounded-[2.5rem] shadow-xl border border-gray-100 flex flex-col hover:border-red-600 transition-all relative overflow-hidden group">
          <Film className="text-red-600 mb-8" size={56} />
          <h3 className="text-3xl font-black mb-4 italic tracking-tight">Film Academy</h3>
          <p className="text-gray-500 mb-10 leading-relaxed">Technical modular workshops for individuals. 8 weeks to go from beginner to self-shot filmmaker.</p>
          <button onClick={() => navigateTo('academy-detail')} className="mt-auto py-5 border-2 border-slate-900 text-slate-900 font-black rounded-2xl uppercase tracking-widest text-[10px] hover:bg-slate-900 hover:text-white transition-colors">View 8-Week Modules</button>
        </div>
      </div>
    </div>
  </div>
);

const CorporateTrainingPage = ({ navigateTo }) => (
  <div className="animate-fade-in pt-32 pb-20">
    <div className="container mx-auto px-6 max-w-6xl">
      <button onClick={() => navigateTo('workshops')} className="text-red-600 font-black flex items-center gap-2 mb-12 hover:-translate-x-1 transition-transform uppercase tracking-widest text-xs">
        <ArrowLeft size={18} /> Back to Workshops
      </button>
      <div className="flex flex-col lg:flex-row gap-20 items-center mb-24">
        <div className="lg:w-1/2">
          <h1 className="text-5xl md:text-7xl font-black text-slate-900 mb-8 leading-none tracking-tighter italic">Corporate <br /><span className="text-red-600">Empowerment.</span></h1>
          <p className="text-xl text-gray-600 leading-relaxed mb-10 font-medium">
            Most businesses have the personnel but lack the technical SOPs. We transform your staff into a high-performance content production unit.
          </p>
          <div className="grid md:grid-cols-2 gap-8">
             <div className="p-8 bg-slate-50 rounded-[2rem] border border-slate-100">
                <CheckCircle className="text-red-600 mb-4" />
                <h4 className="font-black text-xs uppercase tracking-widest text-slate-400 mb-2">Benefit 01</h4>
                <p className="font-bold text-slate-800">Eliminate production bottlenecks by shooting in-house.</p>
             </div>
             <div className="p-8 bg-slate-50 rounded-[2rem] border border-slate-100">
                <BarChart3 className="text-red-600 mb-4" />
                <h4 className="font-black text-xs uppercase tracking-widest text-slate-400 mb-2">Benefit 02</h4>
                <p className="font-bold text-slate-800">Slash marketing costs by 60% compared to agencies.</p>
             </div>
          </div>
        </div>
        <div className="lg:w-1/2 relative">
           <div className="absolute -inset-4 bg-red-600 rounded-[3rem] rotate-2 opacity-5"></div>
           <img src="https://images.unsplash.com/photo-1552664730-d307ca884978?auto=format&fit=crop&w=1600&q=80" className="relative rounded-[2.5rem] shadow-3xl" alt="Training" />
        </div>
      </div>
      
      <div className="bg-slate-900 rounded-[3.5rem] p-16 text-white shadow-3xl">
         <h3 className="text-4xl font-black mb-12 text-center italic tracking-tighter">The Training Curriculum</h3>
         <div className="grid md:grid-cols-2 lg:grid-cols-4 gap-12">
            {[
              { title: "SOP Building", desc: "Custom checklists for capture-to-post workflows." },
              { title: "Brand Sync", desc: "Ensuring all staff content fits the company voice." },
              { title: "Mobile Cinema", desc: "Lighting, audio, and cinematic smartphone movement." },
              { title: "Quality Control", desc: "Approval pipelines and asset management." }
            ].map((item, i) => (
              <div key={i} className="space-y-4">
                 <div className="text-red-600 font-black text-3xl italic">0{i+1}</div>
                 <h4 className="font-bold text-lg">{item.title}</h4>
                 <p className="text-sm text-gray-400 leading-relaxed font-medium">{item.desc}</p>
              </div>
            ))}
         </div>
         <div className="mt-16 text-center">
            <button onClick={() => navigateTo('booking')} className="bg-red-600 text-white px-12 py-5 rounded-full font-black uppercase tracking-widest text-sm shadow-2xl shadow-red-600/30 hover:scale-105 transition-transform">Request Corporate Syllabus</button>
         </div>
      </div>
    </div>
  </div>
);

const BusinessBrandPage = ({ navigateTo }) => (
  <div className="animate-fade-in pt-32 pb-20">
    <div className="container mx-auto px-6 max-w-6xl">
      <button onClick={() => navigateTo('workshops')} className="text-red-600 font-black flex items-center gap-2 mb-12 hover:-translate-x-1 transition-transform uppercase tracking-widest text-xs">
        <ArrowLeft size={18} /> Back to Workshops
      </button>
      <div className="flex flex-col lg:flex-row gap-20 items-center mb-24">
        <div className="lg:w-1/2">
          <h1 className="text-5xl md:text-7xl font-black text-slate-900 mb-8 leading-none tracking-tighter italic">Strategy <br /><span className="text-red-600">for Brands.</span></h1>
          <p className="text-xl text-gray-600 leading-relaxed mb-10 font-medium">
            Learn to treat content like a supply chain. Planned, efficient, and ruthlessly ROI-focused. Turn daily operations into your most powerful marketing.
          </p>
          <div className="space-y-8">
             <div className="flex gap-6 items-start">
                <div className="bg-red-50 p-4 rounded-2xl text-red-600 shadow-sm"><PieChart size={32} /></div>
                <div>
                   <h4 className="font-black text-xl text-slate-900 italic">The "Content Buckets" Method</h4>
                   <p className="text-gray-500 leading-relaxed">Categorize your business wins into Education, BTS, and Testimonial tracks for endless ideas.</p>
                </div>
             </div>
             <div className="flex gap-6 items-start">
                <div className="bg-red-50 p-4 rounded-2xl text-red-600 shadow-sm"><Clock size={32} /></div>
                <div>
                   <h4 className="font-black text-xl text-slate-900 italic">The 1-Hour Content Plan</h4>
                   <p className="text-gray-500 leading-relaxed">A repeatable framework to plan 30 days of high-performance social media in just 60 minutes.</p>
                </div>
             </div>
          </div>
        </div>
        <div className="lg:w-1/2"><img src="https://images.unsplash.com/photo-1460925895917-afdab827c52f?auto=format&fit=crop&w=1600&q=80" className="rounded-[3.5rem] shadow-3xl grayscale hover:grayscale-0 transition-all duration-700" alt="Strategy" /></div>
      </div>
      
      <div className="text-center bg-slate-900 p-20 rounded-[3.5rem] shadow-3xl text-white relative overflow-hidden">
         <div className="absolute top-0 left-0 w-full h-full bg-red-600/5 -skew-x-12 translate-x-20"></div>
         <h3 className="text-4xl font-black mb-6 italic tracking-tighter relative z-10">Stop Posting When You "Remember"</h3>
         <p className="text-gray-400 mb-12 max-w-2xl mx-auto font-medium relative z-10">We teach owners to switch from reactive to proactive marketing, creating a brand that customers can't forget.</p>
         <button onClick={() => navigateTo('booking')} className="bg-red-600 text-white px-12 py-5 rounded-full font-black uppercase tracking-widest text-xs relative z-10 shadow-2xl shadow-red-600/20 hover:scale-105 transition-transform">Inquire for Private Strategy Bootcamp</button>
      </div>
    </div>
  </div>
);

const AcademyDetailPage = ({ navigateTo }) => {
  const [activeModule, setActiveModule] = useState(0);

  const modules = [
    {
      title: "Essentials of Camera and Sound",
      level: "The Foundation",
      overview: "Built for beginners. Learn exposure, focus, color, lenses, and basic professional sound recording.",
      weeks: ["Camera handled & safety", "Exposure Triangle (ISO/Ap/Shut)", "Manual Focus Mastery", "Color & LOG Profiles", "Codecs & Formats", "Gimbal & Tripod Operation", "Basics of Location Sound", "Final Practical Shoot"]
    },
    {
      title: "Directing & Cinematography",
      level: "Narrative Mastery",
      overview: "Scene blocking, actor direction, composition, and visual continuity for storytelling.",
      weeks: ["Director's Vision & Script", "Shot Design & Storyboard", "Working with Actors", "Lighting for Mood", "Cinematic Camera Movement", "Scene Rehearsal Work", "Main Production Day", "Post Review & Critique"]
    },
    {
      title: "Real Estate Media",
      level: "Industry Specialist",
      overview: "Produce cinematic real estate media combining technical precision with visual sales marketing.",
      weeks: ["Industry Standard Overview", "HDR Photography (Bracketing)", "Interior Composition", "Gimbal Walkthroughs", "Drone Basics (Safe Framing)", "Workflow Management", "Editing Property Media", "Client Delivery Prep"]
    },
    {
      title: "Social Media Content",
      level: "Short-Form King",
      overview: "Fast production, strong hooks, and viral energy. Master the art of attention-grabbing vertical video.",
      weeks: ["Platform Trends & AI Behavior", "Short-Form Ideation", "Vertical Frame Framing", "Production Speed Sprints", "Audio Pacing & Mastering", "Brand Voice Storytelling", "Syncing Visuals to Rhythm", "Campaign Launch Strategy"]
    },
    {
      title: "Post-Production & Color",
      level: "The Finishing Artist",
      overview: "The full workflow from raw media to final color grade. Learn to bring rhythm to images.",
      weeks: ["Advanced Setup & Proxy Flow", "Editing Theory & Pacing", "Dialogue Cleanup & Foley", "Sound Design & Scoring", "Color Correction (Balancing)", "Creative Grading (Moods)", "Final Export Standards", "Showcase Screening"]
    }
  ];

  return (
    <div className="animate-fade-in pt-32 pb-20 bg-slate-50 min-h-screen">
      <div className="container mx-auto px-6 max-w-7xl">
        <button onClick={() => navigateTo('workshops')} className="text-red-600 font-black flex items-center gap-2 mb-12 hover:-translate-x-1 transition-transform uppercase tracking-widest text-xs">
          <ArrowLeft size={18} /> Back to Workshops
        </button>
        <div className="mb-16">
           <h1 className="text-5xl md:text-8xl font-black text-slate-900 mb-6 tracking-tighter leading-none italic">ICAN Film <br /><span className="text-red-600">Academy.</span></h1>
           <p className="text-xl text-gray-500 font-medium max-w-3xl">Modular 8-week workshops for individuals ready to master the technical and artistic craft of filmmaking.</p>
        </div>
        
        <div className="flex flex-col lg:flex-row gap-12">
          {/* Module Selector */}
          <div className="lg:w-1/3 space-y-4">
             {modules.map((mod, i) => (
               <button 
                key={i} 
                onClick={() => setActiveModule(i)} 
                className={`w-full text-left p-8 rounded-3xl flex items-center gap-5 transition-all ${activeModule === i ? 'bg-red-600 text-white shadow-2xl translate-x-4' : 'bg-white text-slate-400 border border-gray-100 hover:border-red-300 hover:text-slate-600'}`}
               >
                 <div className="font-black text-2xl opacity-30 italic">0{i+1}</div>
                 <div className="font-black text-sm uppercase tracking-widest">{mod.title}</div>
               </button>
             ))}
          </div>

          {/* Module Detail */}
          <div className="lg:w-2/3 bg-white rounded-[3.5rem] shadow-3xl p-12 md:p-20 relative overflow-hidden">
             <div className="absolute top-0 right-0 p-10"><Award className="text-red-600/5 w-64 h-64 rotate-12" /></div>
             <div className="relative z-10">
                <div className="inline-block bg-red-600 text-white text-[10px] font-black tracking-[0.3em] px-4 py-1.5 rounded-full mb-8 uppercase italic">{modules[activeModule].level}</div>
                <h2 className="text-4xl md:text-6xl font-black text-slate-900 mb-6 italic tracking-tighter leading-none">{modules[activeModule].title}</h2>
                <p className="text-xl text-gray-500 mb-12 pb-12 border-b border-gray-100 font-medium leading-relaxed">{modules[activeModule].overview}</p>
                
                <h4 className="font-black text-xs uppercase tracking-[0.2em] text-slate-300 mb-8 italic">8-Week Detailed Curriculum</h4>
                <div className="grid md:grid-cols-2 gap-x-12 gap-y-6">
                   {modules[activeModule].weeks.map((week, idx) => (
                     <div key={idx} className="flex gap-4 items-center bg-slate-50 p-5 rounded-2xl text-sm border border-slate-100 group hover:border-red-200 transition-colors">
                        <span className="font-black text-red-600 italic">W{idx+1}</span>
                        <span className="text-slate-700 font-black uppercase tracking-tight">{week}</span>
                     </div>
                   ))}
                </div>

                <div className="mt-16 flex flex-col md:flex-row items-center justify-between p-10 bg-slate-900 rounded-[2.5rem] text-white shadow-2xl relative overflow-hidden group">
                   <div className="absolute inset-0 bg-red-600/5 group-hover:bg-red-600/10 transition-colors"></div>
                   <div className="mb-6 md:mb-0 relative z-10">
                      <p className="text-[10px] font-black text-red-500 uppercase tracking-[0.3em] mb-2 italic">TUITION FEE</p>
                      <p className="text-5xl font-black tracking-tighter leading-none italic">$1000 <span className="text-xs font-bold text-gray-500 tracking-widest uppercase">CAD / PP</span></p>
                   </div>
                   <button onClick={() => navigateTo('booking')} className="relative z-10 bg-red-600 px-12 py-5 rounded-full font-black uppercase tracking-widest text-xs shadow-xl shadow-red-600/30 hover:scale-105 transition-transform">Enroll for Next Cohort</button>
                </div>
             </div>
          </div>
        </div>
      </div>
    </div>
  );
};

const BookingPage = () => (
  <div className="animate-fade-in pt-40 pb-24 bg-slate-50 min-h-screen">
    <div className="container mx-auto px-6 max-w-5xl">
      <div className="bg-white rounded-[4rem] shadow-3xl overflow-hidden flex flex-col lg:flex-row border border-gray-100">
        <div className="lg:w-2/5 bg-slate-900 p-16 text-white flex flex-col justify-between relative overflow-hidden">
           <div className="absolute top-0 right-0 w-64 h-64 bg-red-600/10 rounded-full blur-3xl -translate-y-20 translate-x-20"></div>
           <div className="relative z-10">
              <h2 className="text-5xl font-black mb-10 italic tracking-tighter leading-none">Solve the <br /><span className="text-red-600">Overwhelm.</span></h2>
              <div className="space-y-8">
                 <div className="flex gap-4 items-center"><CheckCircle className="text-red-500 shrink-0" /> <span className="text-gray-300 font-bold">24-hour response time guarantee</span></div>
                 <div className="flex gap-4 items-center"><CheckCircle className="text-red-500 shrink-0" /> <span className="text-gray-300 font-bold">Custom audit with 5 specific ideas</span></div>
                 <div className="flex gap-4 items-center"><CheckCircle className="text-red-500 shrink-0" /> <span className="text-gray-300 font-bold">Local Calgary-based experts</span></div>
              </div>
           </div>
           <div className="pt-12 border-t border-white/10 mt-16 relative z-10">
              <p className="text-[10px] font-black text-gray-500 uppercase tracking-[0.4em] mb-4">Direct Inquiry</p>
              <p className="text-2xl font-black tracking-tight">hello@icanfilm.ca</p>
              <p className="text-sm text-gray-500 font-bold">Macleod Trail SW, Calgary, AB</p>
           </div>
        </div>
        <div className="lg:w-3/5 p-16">
          <form className="grid gap-10" onSubmit={(e) => e.preventDefault()}>
            <div className="grid md:grid-cols-2 gap-10">
               <div className="space-y-3">
                  <label className="text-[10px] font-black uppercase text-slate-300 tracking-widest italic ml-1">Full Name</label>
                  <input type="text" className="w-full px-6 py-5 rounded-2xl bg-slate-50 border border-slate-100 outline-none focus:ring-4 focus:ring-red-600/5 focus:border-red-600 transition-all font-bold text-slate-800" placeholder="John Doe" />
               </div>
               <div className="space-y-3">
                  <label className="text-[10px] font-black uppercase text-slate-300 tracking-widest italic ml-1">Business Name</label>
                  <input type="text" className="w-full px-6 py-5 rounded-2xl bg-slate-50 border border-slate-100 outline-none focus:ring-4 focus:ring-red-600/5 focus:border-red-600 transition-all font-bold text-slate-800" placeholder="Calgary Enterprise" />
               </div>
            </div>
            <div className="space-y-3">
               <label className="text-[10px] font-black uppercase text-slate-300 tracking-widest italic ml-1">The Struggle</label>
               <textarea rows="5" className="w-full px-6 py-5 rounded-2xl bg-slate-50 border border-slate-100 outline-none focus:ring-4 focus:ring-red-600/5 focus:border-red-600 transition-all font-bold text-slate-800" placeholder="What is your biggest marketing challenge right now?"></textarea>
            </div>
            <button className="w-full bg-red-600 text-white font-black py-6 rounded-[2rem] shadow-2xl shadow-red-600/30 uppercase tracking-[0.2em] text-sm hover:scale-[1.02] transition-transform flex items-center justify-center gap-3">Send My Inquiry <ArrowRight size={20} /></button>
          </form>
        </div>
      </div>
    </div>
  </div>
);

// --- Main App Component ---

const ICANFilmWebsite = () => {
  const [activePage, setActivePage] = useState('home');
  const [scrolled, setScrolled] = useState(false);
  const [isMenuOpen, setIsMenuOpen] = useState(false);

  useEffect(() => {
    const handleScroll = () => setScrolled(window.scrollY > 50);
    window.addEventListener('scroll', handleScroll);
    return () => window.removeEventListener('scroll', handleScroll);
  }, []);

  const navigateTo = (page) => {
    setActivePage(page);
    setIsMenuOpen(false);
    window.scrollTo({ top: 0, behavior: 'smooth' });
  };

  const navItemClass = (item) => {
    const isAcademyTrack = activePage.includes('-') || activePage === 'academy-detail' || activePage === 'corporate-training' || activePage === 'business-brands';
    const isActive = (item.toLowerCase() === activePage) || (item === 'Workshops' && isAcademyTrack);
    
    return `font-black text-[11px] uppercase tracking-[0.25em] transition-all ${
      scrolled || activePage !== 'home' ? 'text-slate-900' : 'text-white'
    } hover:text-red-600 ${isActive ? 'text-red-600 scale-110 italic' : ''}`;
  };

  return (
    <div className="font-sans text-slate-900 bg-white antialiased overflow-x-hidden min-h-screen flex flex-col selection:bg-red-600 selection:text-white">
      <style>{`
        @keyframes fade-in { from { opacity: 0; transform: translateY(15px); } to { opacity: 1; transform: translateY(0); } }
        .animate-fade-in { animation: fade-in 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards; }
        @keyframes slow-zoom { from { transform: scale(1); } to { transform: scale(1.15); } }
        .animate-slow-zoom { animation: slow-zoom 25s ease-in-out infinite alternate; }
        .italic { font-style: italic; }
        .shadow-3xl { box-shadow: 0 50px 100px -20px rgba(0,0,0,0.25), 0 30px 60px -30px rgba(0,0,0,0.3); }
      `}</style>

      {/* Navigation */}
      <nav className={`fixed w-full z-[100] transition-all duration-700 ${scrolled || activePage !== 'home' ? 'bg-white/95 backdrop-blur-2xl shadow-sm py-4' : 'bg-transparent py-8'}`}>
        <div className="container mx-auto px-6 flex justify-between items-center">
          <div className="cursor-pointer group" onClick={() => navigateTo('home')}>
            <ICANLogo className={scrolled || activePage !== 'home' ? 'text-slate-900' : 'text-white'} />
          </div>

          <div className="hidden md:flex items-center space-x-12">
            {['Home', 'Services', 'Portfolio', 'Workshops'].map((item) => (
              <button key={item} onClick={() => navigateTo(item.toLowerCase())} className={navItemClass(item)}>{item}</button>
            ))}
            <button onClick={() => navigateTo('booking')} className="bg-red-600 text-white px-10 py-3.5 rounded-2xl font-black text-[10px] uppercase tracking-[0.3em] shadow-2xl shadow-red-600/30 hover:scale-105 transition-transform italic">Free Audit</button>
          </div>

          <div className="md:hidden">
            <button onClick={() => setIsMenuOpen(!isMenuOpen)} className={scrolled || activePage !== 'home' ? 'text-slate-900' : 'text-white'}>
              {isMenuOpen ? <X size={32} /> : <Menu size={32} />}
            </button>
          </div>
        </div>

        {isMenuOpen && (
          <div className="md:hidden absolute top-0 left-0 w-full h-screen bg-white z-[120] flex flex-col justify-center gap-12 text-center animate-fade-in p-10">
            <button onClick={() => setIsMenuOpen(false)} className="absolute top-8 right-8 p-4 bg-slate-50 rounded-full"><X size={32} /></button>
            {['Home', 'Services', 'Portfolio', 'Workshops', 'Booking'].map((item) => (
              <button key={item} onClick={() => navigateTo(item.toLowerCase())} className="text-6xl font-black italic tracking-tighter hover:text-red-600 transition-colors uppercase leading-none">{item}</button>
            ))}
          </div>
        )}
      </nav>

      {/* Main Render Logic */}
      <main className="flex-grow">
        {activePage === 'home' && <HomePage navigateTo={navigateTo} />}
        {activePage === 'services' && <ServicesPage navigateTo={navigateTo} />}
        {activePage === 'portfolio' && <PortfolioPage navigateTo={navigateTo} />}
        {activePage === 'workshops' && <WorkshopsPage navigateTo={navigateTo} />}
        {activePage === 'corporate-training' && <CorporateTrainingPage navigateTo={navigateTo} />}
        {activePage === 'business-brands' && <BusinessBrandPage navigateTo={navigateTo} />}
        {activePage === 'academy-detail' && <AcademyDetailPage navigateTo={navigateTo} />}
        {activePage === 'booking' && <BookingPage navigateTo={navigateTo} />}
      </main>

      {/* Footer */}
      <footer className="bg-slate-950 text-slate-500 py-24 border-t border-slate-900">
        <div className="container mx-auto px-6">
          <div className="flex flex-col md:flex-row justify-between items-start gap-16 mb-20">
            <div className="max-w-sm">
              <ICANLogo className="text-white mb-8" />
              <p className="text-sm font-medium leading-relaxed">The only Calgary-based agency combining high-end cinematic production with ruthless social media strategy.</p>
            </div>
            <div className="grid grid-cols-2 gap-20">
               <div className="space-y-6">
                  <h4 className="text-white font-black text-xs uppercase tracking-[0.3em] italic">Ecosystem</h4>
                  <div className="flex flex-col gap-4 text-sm font-bold uppercase tracking-widest">
                     <button onClick={() => navigateTo('home')} className="w-fit hover:text-red-500 transition-colors">Strategic Plan</button>
                     <button onClick={() => navigateTo('services')} className="w-fit hover:text-red-500 transition-colors">The Retainer</button>
                     <button onClick={() => navigateTo('portfolio')} className="w-fit hover:text-red-500 transition-colors">The Proof</button>
                  </div>
               </div>
               <div className="space-y-6">
                  <h4 className="text-white font-black text-xs uppercase tracking-[0.3em] italic">Academy</h4>
                  <div className="flex flex-col gap-4 text-sm font-bold uppercase tracking-widest text-slate-400">
                     <button onClick={() => navigateTo('academy-detail')} className="w-fit hover:text-red-500 transition-colors">Modular Modules</button>
                     <button onClick={() => navigateTo('corporate-training')} className="w-fit hover:text-red-500 transition-colors">B2B Training</button>
                  </div>
               </div>
            </div>
          </div>
          <div className="flex flex-col md:flex-row justify-between items-center pt-12 border-t border-slate-900 gap-8">
             <p className="text-[10px] font-black uppercase tracking-[0.5em]">&copy; 2026 ICAN FILM STRATEGIC BLUEPRINT • CALGARY AB</p>
             <div className="flex gap-10">
                <Instagram size={18} className="hover:text-red-500 cursor-pointer" />
                <Linkedin size={18} className="hover:text-red-500 cursor-pointer" />
                <Youtube size={18} className="hover:text-red-500 cursor-pointer" />
             </div>
          </div>
        </div>
      </footer>
    </div>
  );
};

export default ICANFilmWebsite;
